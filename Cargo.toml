[package]
name = "blank_template"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.19.1", features = ["dynamic_linking"] }

[profile.dev.package."*"]
opt-level = 3

[profile.dev]
opt-level = 0
debug = 1